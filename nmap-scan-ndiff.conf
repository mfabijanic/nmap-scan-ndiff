# Nmap Ndiff
# Add networks for scanning
TARGETS="127.0.0.1 127.0.1.1 127.0.1.2"

# Test - fast mode
#NMAP_OPTIONS="-v -T4 -F -sV"

# Production - all ports
NMAP_OPTIONS="-v -T4 -p1-65535 -sV"


export TARGETS NMAP_OPTIONS

